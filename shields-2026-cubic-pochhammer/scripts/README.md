# Computational supplement — The cubic threshold for coefficientwise log-concavity of multiple-Pochhammer series

Cross-check scripts for *The cubic threshold for coefficientwise
log-concavity of multiple-Pochhammer series*.  The proof in the paper is elementary and
self-contained; these scripts are an independent regression harness.  Every
script re-derives the closed forms it checks from the defining objects — the
series

    F_f(mu; x) = Σ_{n≥1} f_n (mu)_{3n} / (3n-1)! x^n,

the coefficient convolution C_{m,f}(u,v), and the residue kernel G_{m,w} — so
the identities are pinned to their definitions rather than transcribed.
Symbolic work uses SymPy; numerical work uses mpmath at arbitrary precision (no
floating-point arithmetic in the verification loops); combinatorial identities
use exact integer or rational arithmetic.

Where the paper states a hypothesis, a threshold, or a congruence, the harness
also shows it is **load-bearing**, by crossing the boundary and exhibiting the
failure.

The proofs in the paper are noncomputational and do not depend on the code here.

The conjecture given by "Exact computation shows that, for 2 ≤ m ≤ 50, the constant-weight kernel fails to be monotone only at m = 3; we conjecture that degree three is its only failure." is checked by `check_r4_kernel_monotone.py`.

## Installation

Reference environment: Python 3.12, SymPy 1.14.0, mpmath 1.3.0.

```bash
python -m pip install -r requirements.txt
./run_all.sh
```

## Scripts by section

The table below enumerates all scripts, according to where they are used.

| Where | Script | Paper item(s) | Content |
| --- | --- | --- | --- |
| §1, §4, §6 | `verify_theorem.py` | `sec:cubic-proof`, `sec:consequences`, `sec:introduction` | `eq:delta-C` by direct convolution of the two products; the shared parameter sum, ordered deviations `d₁ ≤ d₂`, and the symmetry `C_{m,f}(u,v)=C_{m,f}(v,u)` that the specialization to the Turánian consumes; the vanishing of degrees 0 and 1 and of the Turánian at `α=0` or `β=0`; `sec:introduction`'s hypothesis (nonnegative, interval support, log-concave) and `sec:consequences`'s `f_{n+1}/f_n` nonincreasing (hence `R>0`); `cor:C-schur` (Schur-concavity of the convolution, strict) on the interior `0 ≤ d < s/2` the beta representation reaches, **and** at the endpoint `d = s/2` of its closed range, where `C_{m,f}(s,0)=0` is asserted to sit STRICTLY below every interior value when the weights are not all zero and identically zero when they are; `thm:main` in its Turánian specialization and `cor:strict` (strict classification, both directions, and the `μ=0` branch, which is that endpoint) on random rational log-concave sequences and on the extremal cases (geometric `f`, single-point support); `cor:strict`'s two support descriptions — `I={a..b}` giving exactly the degrees `2a..2b`, `I={a,a+1,…}` exactly `m ≥ 2a`; the `subsec:hypergeometric-specialization` triplication identity `F_f = 3x (d/dx) ₃F₂` for `f_n≡1`; `cor:ordinary` (the Stirling ratio with a fitted `O(1/n)` rate including the exact case `μ=1`, the shared radius of convergence for constant and geometric `f`, continuity in `μ`, the local uniform convergence its proof needs — the sup over the proof's own closed `0 ≤ μ ≤ M`, endpoint `μ=0` included, of the series tail decaying to zero under the majorant `M e^M (3n)^M` — and **strict** midpoint-hence-strict concavity of `μ ↦ log F_f(μ;x)`). |
| §1, §1.1 Thm 1.1, … §6.3 Cor 6.2 | `check_fixed_sum_schur.py` | `sec:introduction` (`thm:main` fixed-sum form), `sec:cubic-proof` (`cor:C-schur`, `cor:strict`), `sec:consequences` (`cor:ordinary`, `cor:differential`), `sec:reduction` (`eq:G-weighted`, `lem:beta-order`, `prop:kernel-exact`) | The points at which the fixed-sum statement is stronger than the Turánian one, each by a route independent of the paper's proof. `eq:fixed-sum` itself, which `cor:C-schur` runs through: `κ_{m,s} = (s)_{3m}/(3m)! · 3m(3m-1)` positive for `s>0` and zero at `s=0`; the folding `G_{m,w}(p) → Ĝ_{m,w}(q)` at `q=p(1-p)` shown **well defined** for symmetric weights by reduction modulo `p²-p+q`, with an asymmetric witness where the reduction stays `p`-dependent; and `C_{m,w}(s/2+d, s/2-d) = κ_{m,s} E Ĝ_{m,w}(Q_d)` symbolically in `(u,v)` at symbolic weights. `eq:delta-C` at its own parameters — the degree-`m` coefficient of the series product at `(s/2+d, s/2-d)` convolved directly and compared against `C_{m,f}` there, which is the one place the script's other, `C`-level checks would otherwise rest on an unverified extraction (`verify_theorem.py` asserts the same identity, but only at the Turánian pairs). The normalization of `eq:C-beta` split into a factorial identity and a Beta moment, neither mentioning the weights — the form `cor:C-schur` is stated in; the fixed-sum map strictly decreasing on the interior `[0,s/2)` for arbitrary monotone symmetric weights, including vectors with interior zeros, and `eq:w-monotone` load-bearing (the `w=(1,0,0,1)` of rem:internal-zeros reverses the order, `-9360` at `s=6` — negative for every tested `s≥5`, positive for `s≤4`, so the failure needs the parameter sum as well as the weight order); the endpoint `C_{m,w}(s,0)=0`; the specialization reproducing `eq:delta-C` over the whole closure `μ=0`, `αβ=0`, `s=0`, with `s/2-d₂=μ` identically so the endpoint is reached exactly at `μ=0`; `cor:strict`'s exact support `I+I` in both the interval and infinite forms over eight supports; the **active-degree dichotomy** of `cor:C-schur` and 4.2 — `a_m(d)` strictly decreasing on the *closed* `[0,s/2]` for `m ∈ I+I` and identically zero (pointwise, not merely constant) for `m ∉ I+I`; `cor:ordinary`'s strict four-point inequality and strict concavity, with the `x^{2n_0}` witness at a supported index; `cor:differential`'s `h`-expansion starting at `h²` (the `O(1)` and `O(h)` terms cancelling by `k ↔ m-k`) with `h²` term exactly `(∂_μF)² - F ∂²_μF` and value `Σ_k f_k f_{m-k}` at `μ=0`, and that coefficient shown `> 0` **exactly** on `I+I` — the strict equivalence the corollary states, with degrees inside and outside `I+I` both represented, where supports whose `I+I` covers every degree tested could only exercise `≥ 0`; `rem:local-weight`'s degree-local chain, satisfied at `m=6` by the non-log-concave `f=(1,1,5,1,1)` whose `m=4` chain fails along with its `m=4` conclusion; the gap-length refinement of rem:internal-zeros's witness (one intervening zero violates log-concavity at that zero, two do not, at every tested position); and `lem:beta-order`'s likelihood-ratio conclusion, with each `g_d` checked to be a density and a symmetric monotone `H` shown bounded, so no integrability hypothesis is needed. |
| §2 | `verify_beta_binomial.py` | `sec:reduction` | Symmetry and center-monotonicity of the paired weights `w_k = f_k f_{m-k}` from log-concavity, via the cross-product `f_{a+1}f_b ≥ f_a f_{b+1}`; the **converse** of `lem:central-products`, that `eq:central-products` at every `m` forces log-concavity and interval support — by the paper's own `m=2j` and `m=a+b` specializations, and as the full equivalence over every nonzero sequence in `{0..3}^5`, both sides of which occur; the coefficient convolution `C_{m,w}` and its vanishing at `u=0` **and** at `v=0`; the Pochhammer/beta-binomial weight identity (`eq:C-beta-binomial`); the endpoint identity `j(N-j)C(N,j)=N(N-1)C(N-2,j-1)`; that the beta-binomial pmf is a law (Chu–Vandermonde); `eq:C-beta-binomial` as an assembled equation, with `J(N-J)` making the endpoint weights irrelevant; the mixture identity `C_{m,w} = (u+v)_N/N! · N(N-1) · E G_{m,w}(P)` (`eq:C-beta`–`eq:G-weighted`) as a single symbolic identity at the general-weight form those equations are stated in, with its binomial read off `G_weighted` rather than retyped; the `p ↔ 1-p` symmetry of `G_{m,w}`. |
| §2 Lem 2.1, §3, … §5.1 | `verify_multiplicity.py` | `sec:threshold`, `subsec:first-supercritical-case` (`prop:multiplicity-threshold`), `sec:reduction` (`lem:central-products`), `sec:kernel` (`lem:bernstein`) | The `sec:threshold` sweep. `eq:r-degree-three`, the degree-three generalized Turánian at arbitrary multiplicity `r`, against the direct convolution symbolically in `μ` for `r=2..15`, and its `r=4` factorization; its critical factor `r(4r−(r−3)μ)` classifying the multiplicities, with the `r=2,3` branches settled by **coefficients** — `2μ+16` and the constant `36` — rather than at five sampled `μ`, and the whole coefficient negative past `μ > 4r/(r−3)` for `r=4..40, 100, 1000`, that threshold being `μ>16` at `r=4`; the two-term witness `f=(1,1,0,…)`, checked to be log-concave with interval support and to give the same coefficient. `eq:r-central-slope`, the kernel's slope in the centrality coordinate at `q=1/4`, equal to `C(3r−2,r−1) r(3−r) 2^{2−3r}` — positive at `r=2`, **exactly zero** at `r=3`, negative for `r=4..13` — with the `r=3` kernel `21q³(1−3q)` rising on `(0,1/4)`; the quartically flat maximum at `r=3`. `prop:multiplicity-threshold`'s two minimality clauses over the **whole cone** at once: `(μ+α)_N(μ+β)_N − (μ)_N(μ+α+β)_N` has every coefficient positive as a polynomial in `(μ,α,β)` and no constant term for `N=1..11`, so both degree two and one-point support are settled including at the paper's own `α=β=1`, with the sampled grids kept as cross-checks. Then the general-`r` reduction: the control quantity `G_m(p) = p(1-p) P{Bin(3m-2,p) ≡ 2 (mod 3)}`; log-concavity as the load-bearing hypothesis for the weight monotonicity (the implication is one-directional at fixed `m`, with a witness); the general-`r` kernel `= p(1-p) P{Bin(rm-2,p) ≡ r-1 (mod r)}` for `r=2,3,4`, no other residue class, and its weighted analogue with symbolic `w_k`; that positivity is not termwise in the monomial basis; `J^{(4)}_3(t) = 120t³(1−2t+2t⁴−t⁵)` and its equivalent factored form — script-side extras the paper displays nowhere — with the projective lift's degree read off the polynomial and its top coefficient identified as `J^{(r)}_m(1)`; `J^{(3)}_m>0` on `(0,1)` for `2≤m≤12` by exact root count while `J^{(4)}_3` has an interior sign change, and over `4≤m≤22` `J^{(4)}_m` has no interior root and is positive throughout, so there is no shrinking-interval phenomenon — evidence for the question `sec:conclusion` leaves open, the proved part of that discussion being in `check_r4_obstruction.py`, which owns the lifted array and its closed forms in `m`; and the explicit `r=4` counterexample for `f_n=1` (`m=3`, `α=β=1`), whose exact factorization puts the threshold at `μ=16` — zero there, negative for every real `μ>16`, `-87679680` at the smallest integer witness `μ=17` — reproducing Karp–Zhang's Remark 5 by three independent routes while the `r=3` Turánian stays `≥0`. |
| §5.1 | `check_r4_obstruction.py` | `subsec:first-supercritical-case` (The first supercritical case), the `r=4` obstruction | Where `verify_multiplicity.py` shows *that* the residue-class certificate fails at `r=4`, this shows *how*, exactly and uniformly in `m`. The lifted coefficients `a_7,a_8,a_9` of `(1+ξ)^{4m-1}J^{(4)}_m(ξ/(1+ξ))` see only `k≤2` whatever `m` is (the next contribution sits at exponent `11>9`), so their closed forms are identities rather than fits: `a_7=8Q/315`, `a_8=-28Q/315`, `a_9=4Q(2m-11)(4m-9)/945` with `Q=(m-2)(m-1)(2m-3)(2m-1)(4m-7)(4m-5)(4m-3)`, checked against the integer lift out to `m=140`. Hence `a_8=-(7/2)a_7` identically, and `a_8<0` for **every real** `m≥3`: substituting `m = 3+s` puts each of `Q`'s seven factors into a polynomial in `s` with nonnegative coefficients and a positive constant term, so the certificate fails at `r=4` at every degree from the third on rather than at the swept ones. The boundary is asserted rather than assumed: at `m=2` the lift is `20ξ^3+60ξ^4+60ξ^5+20ξ^6`, all coefficients nonnegative, and the closed forms give `a_7=a_8=0` through their factor `(m-2)`, so the certificate *does* hold there and `m≥3` is the exact scope. The closed forms are also pinned against the full `m=3` lift (`240,-840,-600` at indices 7, 8, 9), an internal consistency pin; the paper prints no such array. Then `subsec:first-supercritical-case`'s other direction, that the monotonicity recovers at the next degree: every step of the `m=4` certificate — the factorization `J^{(4)}_4=52t^3(1-t)W(t)`, the reciprocity of the octic `W`, the two substitutions `y=t+1/t` and `z=y-2=(1-t)^2/t`, and the regrouping `t^{-4}W=7z^4+42z^3+42(z-1)^2+6` that clears the negative linear term — with the interior root count as an independent route, none in `(0,1)` at `m=4` against exactly one at `m=3`. Carried alongside as a lead for the all-`m` target rather than a paper claim: `4a_7a_9-a_8^2=(16/297675)Q^2(64m^2-496m+645)`, so the three-term group is nonnegative on `ξ≥0` **exactly** for `m≥7`, settled by shifting to `m = 7+u`, where the quadratic reads `64u²+400u+309` and every coefficient is positive, so the sign is fixed for every **real** `m ≥ 7` rather than at swept integers, with the largest root `(31+2√79)/8` placed in `(6,7)`; and pointwise at the group's exact rational minimizer, the `a_9 ≤ 0` branch evaluating the group at `ξ = 10⁶` and asserting it negative. The `r=3` contrast — every lifted coefficient nonnegative for `2≤m≤20` — is asserted alongside. Exact throughout: `sympy` over rationals and `math.comb` over integers, no float. |
| §5.1, §7 | `check_r4_kernel_monotone.py` | `sec:conclusion` (Concluding remarks), `sec:threshold` (`prop:multiplicity-threshold`), `subsec:first-supercritical-case` | The degreewise scope of the multiplicity-four failure, which is the evidence for the conjecture the conclusion states. Monotonicity is read in the centrality coordinate: `G^{(r)}_m` is symmetric about `p = 1/2`, so with `u = p - 1/2` it is even in `u` and equals `H(u^2)`, and since `q = 1/4 - u^2` the kernel is nondecreasing toward the centre exactly when `H' <= 0` on `[0,1/4]`. Evenness in `u` is asserted rather than assumed, and the test is an exact Sturm root count over rationals rather than sampling, so a missed root is not a possible failure mode. Establishes that `G^{(3)}_m` is monotone for `m = 2..50` (the direction `thm:kernel` proves), that at `r = 4` degree three is the ONLY non-monotone degree over the same range and carries exactly one interior turning point, and that `r = 4, m = 4` is monotone, which `subsec:first-supercritical-case` proves by hand through `J^{(4)}_4 > 0`. |
| §2 Lem 2.1, §3, … §6.2 Cor 6.1 | `check_proof_steps.py` | `sec:kernel` (`lem:bernstein`), `sec:reduction` (`lem:central-products`), `sec:cubic-proof` (`cor:C-schur`), `sec:consequences` (`cor:ordinary`) | The four displays that state a proof *step* whose conclusion another script already covers. `lem:bernstein`'s second Bernstein coefficient at its closed value `b_2=(n-1)/(n+1)`, with `3S_{n,2}=9(n-1)` behind it and the expansion `Σ_j b_j C(n+1,j)t^j(1-t)^{n+1-j}` reconstructing `J_m` exactly — `verify_kernel.py` carries `b_j=S_{n,j}/(3(n+1))` and the positivity of the `ξ²` coefficient, so the closed value is what is new — together with `b_{n+1}=0`, the top index the congruence `n+1 ≡ 2,5 (mod 6)` keeps out of the table's negative branches. `thm:main`'s route to `eq:w-monotone` through the ratios `ρ_j=f_{j+1}/f_j`: the identity `w_{k+1}/w_k=ρ_k/ρ_{m-k-1}` symbolically, the index bound `k ≤ m-k-1` the comparison needs, `eq:w-monotone` itself over log-concave interval-support families (each checked to *be* log-concave with interval support before it is used, since `f_n=1/n` is log-convex and belongs to no such list), and the step shown load-bearing in the other direction: at rem:internal-zeros's `f_1=f_4=1` log-concavity still holds at every triple, `ρ_1` is unavailable because `f_2=0`, and `eq:w-monotone` fails outright — so interval support is consumed at exactly this step. `cor:C-schur` on the **closed** range it is stated on, for weights built from a half-vector so symmetry holds by construction and then checked against `eq:w-monotone`, strict when the weights are not all zero and flat when they are all zero, with endpoint value `0`. And `cor:ordinary`'s two analytic steps: finite support giving `R=∞` — its partial sums stopping dead at the top of the support however large `x` is, against a geometric `f` past its radius whose partial sums do not — and `f_n ≤ f_a(f_{a+1}/f_a)^{n-a}` on infinite support, over strictly log-concave families where the bound is a real inequality, with the geometric equality case asserted separately; and the explicit majorant `(μ)_{3n}/(3n-1)! = μ∏_{j<3n}(1+μ/j) ≤ Me^M(3n)^M` on `0 ≤ μ ≤ M`, with `n^M(x/y)^n` peaking at an interior index and its tail vanishing so the Weierstrass majorant `C f_n y^n` is summable. Exact `sympy` and integer arithmetic; the one transcendental comparison, against `Me^M`, is `mpmath` at 60 digits, and there is no float64 anywhere. |
| §2 eq. (2.4), §2 Lem 2.3, §4 Cor 4.1 | `check_beta_moment_fold.py` | `sec:reduction` (`eq:C-beta`, `lem:beta-order`), `sec:cubic-proof` (`cor:C-schur`) | section `sec:reduction`'s reduction by a route that never leaves the `p`-coordinate and never names a probability law — the route `../lean/CubicPochhammer/BetaOrder.lean` and `Bridge.Aint_eq` take, so what is checked here is what is load-bearing there. The beta-moment shift `B(a+j,b+k)(a+b)_{j+k} = (a)_j(b)_k B(a,b)`, the only property of the beta function the reduction consumes, symbolically in `(a,b)` over 81 shift pairs. `eq:C-beta` with the normalizations cleared, `A(u,v)(u+v)_{3m} = (3m-2)! B(u,v) C_{m,w}(u,v)` where `A` is the kernel against the unnormalized `Beta(u,v)` weight: by quadrature against `eq:C-def`, and again exactly once the integral is expanded term by term, symbolic in `(u,v)` and in the weights — with asymmetric weights included, since this half needs no symmetry. The fold onto `[1/2,1]`, computed by a **different** quadrature rule from the unfolded side so the identity is not compared against itself, with the symmetry of `G_{m,w}` shown load-bearing (asymmetric weights break it by 5–6%). The folded kernel's closed form `2(p(1-p))^{s/2-1} cosh(d·logit p)`, which is where `cosh` enters without the change of variables `q=p(1-p)`. The cross-multiplied inequality `cosh(d₂x)cosh(d₁y) ≤ cosh(d₂y)cosh(d₁x)`, together with the two `\|
| §2 Lem 2.3, §3, §3.2 Lem 3.3 | `verify_monotonicity_lemmas.py` | `sec:kernel` (`lem:weighting`), `sec:reduction` (`lem:beta-order`) | `lem:weighting` as the paper states it: `eq:abel-weight` symbolically for `L = 1..8` and on unrestricted rational draws (the identity needs no hypothesis on `w` at all); **both directions** of the cone equivalence, the reverse carried by the step weight `1_{k≥σ}`, which recovers an individual tail and detects a negative one; the strict cone clause, with the excluded all-zero weight appearing in the sweep so that "nonzero" is tested rather than assumed; and the one-sign-change corollary in both its nonstrict and strict halves, with the two boundary shapes (`Σ A_k = 0`, `A_L = 0`) random draws never produce. Each hypothesis has a negative control: two sign changes, a single change in the wrong direction, weights that decrease (`w=(5,1)`, `A=(-1,11/10)`, weighted sum `-39/10` against two positive tails) and weights that go negative; and each of the strict clause's three added hypotheses admits an admissible pair with weighted sum zero when dropped. `lem:beta-order` (symmetric beta imbalance ordering): the closed-form density of `Q_d = P_d(1-P_d)`, monotonicity of `ℓ(q)` and of `cosh(d₂ℓ)/cosh(d₁ℓ)` — both swept to the scale the file's own load-bearing case sits at, `ℓ(2·10⁻⁵) = 10.82`, not to `ℓ ≤ 4` — the swept `(s, d₁, d₂)` cases asserted to reach past the gap restriction `2d ∈ {0} ∪ [1,∞)` that the cited folded-beta comparison carries — three of the four lie outside it and one has **both** gaps below 1, so the wider range `0 ≤ d₁ < d₂ < s/2` is a tested claim rather than an asserted one; the single crossing of `g_{d₁}-g_{d₂}` on a log-refined grid (the crossing can sit at `q ≈ 2·10⁻⁵`, so the grid has to reach that scale — itself asserted), the proof's integrand nonnegative *throughout* `(0,1/4)`, `E H(P_{d₁}) ≥ E H(P_{d₂})` (`eq:beta-order`), and both of its hypotheses on `H` given counterexamples: `H(p)=p` is nondecreasing but not symmetric and reverses `eq:beta-order` by `-4/15`, `H(p)=max(p,1-p)` is symmetric but decreasing and reverses it too. |
| §2 Prop 2.4, §3, … §6.3 Cor 6.2 | `check_structural.py` | `sec:reduction` (`prop:kernel-exact`), `sec:kernel` (`eq:abel-weight`), `sec:consequences` (`cor:differential`) | The three structural results; `sec:threshold`'s headline identities are in `verify_multiplicity.py`, the sweep named for that section. `eq:abel-weight`, the Abel/tail-sum identity, and the dual cone it exhibits, **exhaustively** for `L≤5`: nonnegative against every monotone weight exactly when every tail sum is nonnegative, with the failing **central-window indicator** as the witness when one is not, and one sign change with a nonnegative total shown to force every tail nonnegative (the lemma as stated is in `verify_monotonicity_lemmas.py`). `prop:kernel-exact` in **both** directions: the equivalence between monotonicity of `G_{m,w}` on `[0,1/2]` and Schur-concavity of `C_{m,w}`, over eleven symmetric weight vectors including `eq:w-monotone` violators, with the first side decided by an exact interior root count of `J_{m,w}` and the second by an explicit increasing pair wherever it fails; and the converse's concentration step as the symbolic limit `s → ∞` of the exact beta-moment sum, which returns `G(p)` identically in `p` rather than as a list of decreasing errors — those are kept as a cross-check, together with the contrapositive on the non-central `r=4, m=3` kernel, whose all-scale gap goes negative. And `cor:differential`'s exact support: strict positivity precisely on `I+I` at `μ=0,1/2,1,7,50` over five supports, the identity `[x^m](…) = −Φ''(0)/2`, the `μ=0` degeneration to `(Σ f_n x^n)²`, and `Φ''(0) = κ_{m,2μ} Cov(Ĝ_{m,w}(Q₀), ℓ(Q₀)²) < 0` at `m=3..6` and at weights from a log-concave `f`, with `κ` asserted from `eq:fixed-sum` and every quadrature taken with its error estimate, itself asserted below a millionth of the covariance it bounds — the density is singular at both endpoints and the conclusion is strict. Exact `sympy` and integer arithmetic; `mpmath` at 30 digits for the covariance integrals. |
| §3 | `verify_kernel.py` | `sec:kernel` | The `t`-substitution of `G_m` with `0≤p≤1/2 ⇔ 0≤t≤1` and the derivative identity (`eq:G-J`–`eq:J-k`); the projective transform `P_m` (`eq:P-def`–`eq:P-sum`); the coefficient formula (`eq:P-coeff`), the two binomial identities, `S_{n,j}` (`eq:S-def`), the root-of-unity counts by three routes (table, trigonometry, and the `ω`-filter in exact `Z[ω]`) plus a ℂ-free Pascal recurrence as an independent fourth, the moments, the R-expansion (`eq:S-R`), and the six-case table (`eq:S-table`); the exponential bounds (`eq:exp-bound-1`–`eq:exp-bound-0`), their failure one step below range, and that both are **attained** at `m=3` (`S_{7,6}=S_{7,7}=0`); nonnegativity of every `S_{n,j}` and that the congruence `n+1 ≡ 2,5 (mod 6)` is **load-bearing** — without it `S_{n,j}` goes negative (first `S_{5,6}=-30`, `S_{6,6}=-15`); the Bernstein reconstruction of `J_m`, the closed value `b_2=(n-1)/(n+1)` asserted over the whole range `m=2..199` rather than only where the reconstruction is affordable, `J_m>0` and `G_m` strictly increasing; the weighted blocks `B_{m,k}` (`eq:B-def`–`eq:B-center`, with the center block's `m/2` derived from `eq:Jw-def`), the hyperbolic form (`eq:H-hyperbolic`), the single sign change with `B_k≤0 ⇒ B_{k-1}<0` (`lem:block-sign`); the **central-window tails** `T_{m,σ} = Σ_{k≥σ} B_{m,k}` that `thm:kernel`'s proof actually runs on — positive for every `1 ≤ σ ≤ ⌊m/2⌋` and `0<t<1` in exact rationals over `m=2..20`, with `T_{m,1}=J_m` and each tail identified as the `J`-numerator of `1_{k≥σ}`; `thm:kernel` itself; that **both halves** of `eq:w-monotone` are load-bearing — decreasing weights drive `J_{m,w}<0`, and dropping symmetry does too, `w=(1,0)` at `m=3` satisfying the ordering hypothesis outright and giving `J_{3,w}(3/5) = -189/125` — and that `eq:w-monotone` is strictly weaker than log-concavity of the weights; the `B_{m,k}` domain guard, with `2k=m ⇒ m` even asserted rather than guarded; and the scope edges (`G_{m,w}` decreases on `[1/2,1]`; `J_1=0` from `eq:Jw-def`'s own variable bound, so `m≥2` is needed for strictness). |
| §3.1 Lem 3.2 | `check_kernel_stdlib.py` | `subsec:constant-weight-kernel`, `lem:bernstein` | Dependency-free integer audit (standard library only, no float) of the positivity certificate `S_{n,j} ≥ 0`, `eq:P-coeff`–`eq:S-R`, the `δ` branch bounds, and the load-bearing congruence, over a wide range of `m`. Run `python3 check_kernel_stdlib.py [MAX_M]` (default 120). Scope: the certificate and `eq:P-coeff`–`eq:S-R`; `J_m` itself, the link back to `P_m` through `eq:P-def`–`eq:P-sum`, and the general inequalities of `eq:exp-bound-1`–`eq:exp-bound-0` with their increments, are in `verify_kernel.py`. |
| §5 Prop 5.1, §5 Fig 1 | `make_figure_multiplicity.py` | `sec:threshold` (`prop:multiplicity-threshold`, `fig:multiplicity-threshold`) | Every claim `fig:multiplicity-threshold` and its caption make, and the `pgfplots` coordinate blocks the figure inlines. `G^{(r)}_3` re-summed from the section's constant-weight kernel at `m=3` rather than transcribed, against the closed form `C(3r-2,r-1)[p(1-p)]^r(p^r+(1-p)^r)`, its `q`-form, and the central value `Ghat^{(r)}_3(1/4) = C(3r-2,r-1) 2^{1-3r}` that normalizes the plot.  Dividing by that value turns `eq:r-central-slope` into the endpoint slope `2r(3-r)` **exactly** — the binomial and the power of two cancel — giving `4, 0, -8, -20` at `r=2,3,4,5`, and leaves the integer prefactor `2^{3r-1}`; both are checked for `r=2..12`.  The three plotted shapes are asserted separately: no interior maximum and a positive derivative throughout `(0,1/4)` at `r=2,3`, so the central endpoint is the maximum; and interior maxima at exactly `5/6-sqrt(13)/6` and `3/7-sqrt(2)/7` at `r=4,5`, with the overshoots the caption prints — read out of the caption and compared as exact roundings, not as a prefix of the printed digits.  The tangency is tied back to the proposition rather than eyeballed: `1/4-q=(p-1/2)^2`, so the normalized `r=3` kernel is `1-96y^2+512y^3-768y^4` in `y=1/4-q` and `1-96x^4+512x^6-768x^8` in `x=p-1/2`, which is the quartically flat expansion divided by `21/256`.  The figure's own marker literals are checked to be those maxima correctly rounded; **every one of the 484 inlined curve coordinates** is parsed back out of the manuscript and required to match the block `emit()` builds, character for character, so `emit()` formats both sides; and no plotted sample falls outside the axis limits, which are read out of the figure's own `axis[...]` options rather than retyped here.  Run with `--emit` to print the blocks.  Exact `sympy` over the rationals, with roundings done by `sympy`'s floor rather than through float64; sampling is `mpmath` at 40 digits. |
| §5 Fig 1 | `render_figures.py` | `sec:threshold` (`fig:multiplicity-threshold`) | Not a check but a submission artifact: it builds the separate artwork file `shields-2026-cubic-pochhammer-figures.pdf` the portal wants, one figure per page, by extracting the `pgfplots` axis from the manuscript itself rather than from a hand-kept copy — so the artwork cannot drift from the paper. The governing invariant is that the number of figure floats in the manuscript equals the number of pages rendered, checked against the built PDF, so a float added to the paper and forgotten here fails this script instead of silently shipping incomplete artwork; the `\addplot` count (over the uncommented body, so a curve disabled with a leading `%` is caught), the axis and legend landmark text, and a floor on the vector path segments are asserted alongside, and a raster image on the page is an error. What the figure *claims* is verified by `make_figure_multiplicity.py`, which owns the coordinates. The float-count and plot-count invariants are pure Python and always run; the render itself is skipped with a message where `pdflatex` or `PyMuPDF` is absent, since `requirements.txt` pins only `sympy` and `mpmath`. |
| §6.3 Cor 6.2 | `check_differential_domination.py` | `sec:consequences` (`cor:differential`), `sec:reduction` (`lem:beta-order`) | The interchange behind differentiating `eq:fixed-sum` twice under the integral, which `cor:differential`'s proof settles in a sentence. `g_d` is singular at **both** ends of `(0,1/4)` and the ends need different estimates, so this is a two-endpoint argument and the script checks both. Near `0`: `ell(q) = log(1/q) + O(q)`, with the difference over `q` measured at exactly `2`; `cosh(d ell(q)) q^{|d|}` two-sidedly bounded and falling to `1/2` down to `q = 1e-24`, so the singular factor there is a **power**, not a logarithm; and `g_d` with its first two `d`-derivatives bounded by `q^{mu-1-d0} log^2(1/q)` uniformly on `|d| <= d0 < mu`. Near `1/4`: `ell(q)/sqrt(1-4q) -> 2`, and the same quantities are bounded by `(1-4q)^{-1/2}`. Both majorants integrate. **Neither covers the other end, and that is asserted rather than assumed**: the `q -> 0` majorant is exhibited *failing* near `1/4`, by a ratio rising past `2285` and unbounded, so a one-endpoint argument is not merely incomplete but false. The compactness is separately load-bearing — at `d0 = mu` the first exponent is exactly `-1` and that majorant diverges. `ell` is rationalized as `log((1+r)^2/(4q))` because the literal `(1+r)/(1-r)` cancels catastrophically and swamps the `O(q)` term being measured. `check_structural.py` owns `Phi''(0)` and its sign; `verify_monotonicity_lemmas.py` owns the closed form of `g_d`. `mpmath` at 40 digits, no float64. |
| §6.3 Cor 6.2 | `check_differential_coefficients.py` | `sec:consequences` (`cor:differential`), `sec:kernel` (`eq:w-monotone`) | The **finite** route to the degree-`m` coefficient, with no integral anywhere, which is the route `../lean/CubicPochhammer/Differential.lean` takes. `-Φ''(0)/2 = ½ Σ_k u_k (V_k - T_k²)` with `u_k` the terms of `C_{m,w}(μ,μ)`, `T_k = H(3k) - H(3(m-k))` and `V_k = S(3k) + S(3(m-k))`, checked against the exact `d`-derivative at every extreme ray of the `eq:w-monotone` cone. The inequality `Σ u_k T_k² < Σ u_k V_k` — the statement the Lean tree records as its remaining gap — verified TRUE over `m = 2..50`, every extreme ray and four staircases, fifteen values of `μ`, with `E[T] = 0` throughout. Then **three facts about how hard it is**, each asserted rather than remarked: it is not pairwise (`(T_1 - T_{m-1})² = 49.3` against `V_1 + V_{m-1} = 6.0` at `m = 40`, `μ = 1`), so no per-term argument can reach it; it is asymptotically tight (slack `2/μ²` against sides `3m/μ²`, so relative margin `2/(3m)`); and the `j = 0` term carrying that slack is **not** separable — dropping it from both sides reverses the inequality at `m = 40`, `μ = 5`. The **Abel reduction**: the tail sums `B_j` of `lem:weighting` are unimodal in `j` (`sign(B_{j+1}-B_j) = -sign(tau(3j))` with `tau` increasing), so the minimum sits at `j = 1` or `j = floor(m/2)` and both occur as `mu` varies — `B_1` is the coefficient at the constant sequence `f = 1`, checked exactly, and `B_{floor(m/2)}` is the central window. So strictness for the whole `eq:w-monotone` cone reduces to those two. The summand also **collapses**: `(V_k - T_k^2)/2 = tau(3 min(k, m-k))` with `tau(n) = S(n) - e_2([n, 3m-n))`, so it depends on `k` only through the length of the shorter index block, and `tau` is strictly increasing with a single sign change — so the missing statement is that a single-crossing function has positive mean, the shape `lem:weighting` and `lem:beta-order` already take. Finally the exact Wronskian identity `((μ)_N')² - (μ)_N (μ)_N'' = Σ_{i<N} (∏_{j≠i}(μ+j))²` behind the `m = 2` closure, and strict positivity of **every** `μ`-coefficient of the degree-`m` coefficient at every extreme ray for `m = 2..9`, the smallest being `1801/2497779395061350400000` — the cancellation a coefficientwise proof would have to reproduce. `check_differential_domination.py` owns the integral route's interchange; `check_structural.py` owns `Φ''(0)` and its sign. Exact `sympy` for every identity, `mpmath` at 30 digits for the sweeps. |

Each script prints `PASS` lines and ends with `ALL PASS`; any broken identity
raises `AssertionError` and stops the script with a nonzero exit code.
`./run_all.sh` runs them in dependency order and exits nonzero on the first
failure.

The mutation tester that confirms each assertion above can fail is
`_drafting/check_mutation_bite.py`, run by hand.

## Coverage by section

The table below lists every item the paper states and indicates its coverage by
the scripts above, in the order the items appear in the paper.

| § | Paper item | Kind | Coverage | Python (file:line) | Notes |
|---|---|---|---|---|---|
| §1.1 eq. (1.1) | `eq:general-r` | equation | - | verify_multiplicity.py:148 | Definition fixing notation; the family's kernel identity is asserted for `r = 2,3,4`. |
| §1.1 Thm 1.1 | `thm:main` | theorem | ✅ Full | verify_theorem.py:366 | — |
| §1.1 eq. (1.2) | `eq:F-def` | equation | - | — | Definition fixing notation. |
| §1.1 eq. (1.3) | `eq:schur-def` | equation | ✅ Full | check_fixed_sum_schur.py:453 | — |
| §1.1 eq. (1.4) | `eq:Turan-def` | equation | ✅ Full | verify_theorem.py:124 | Pinned through `eq:delta-C` rather than at its own display. |
| §2 Lem 2.1 | `lem:central-products` | lemma | ✅ Full | check_proof_steps.py:97 | Both directions; interval support shown load-bearing. |
| §2 eq. (2.1) | `eq:central-products` | equation | ✅ Full | check_proof_steps.py:141 | — |
| §2 Rmk 2.2 | `rem:internal-zeros` | remark | ✅ Full | verify_beta_binomial.py:148 | — |
| §2 eq. (2.2) | `eq:C-def` | equation | ✅ Full | verify_beta_binomial.py:181 | — |
| §2 eq. (2.3) | `eq:C-beta-binomial` | equation | ✅ Full | verify_beta_binomial.py:214 | — |
| §2 eq. (2.4) | `eq:C-beta` | equation | ✅ Full | verify_beta_binomial.py:214 | Second independent route in `check_beta_moment_fold.py`. |
| §2 eq. (2.5) | `eq:G-weighted` | equation | ✅ Full | verify_beta_binomial.py:371 | — |
| §2 eq. (2.6) | `eq:fixed-sum` | equation | ✅ Full | check_fixed_sum_schur.py:206 | — |
| §2 Lem 2.3 | `lem:beta-order` | lemma | ✅ Full | check_beta_moment_fold.py:367 | Both hypotheses shown sharp by counterexample. |
| §2 eq. (2.7) | `eq:beta-order` | equation | ✅ Full | verify_monotonicity_lemmas.py:616 | — |
| §2 Prop 2.4 | `prop:kernel-exact` | proposition | ✅ Full | check_structural.py:139 | Both directions, including the `s → ∞` concentration as a symbolic limit. |
| §2 Rmk 2.5 | `rem:fixed-total` | remark | ✅ Full | verify_beta_binomial.py:344 | The conditional law, checked symbolically in `u`, `v` and `x`. |
| §3 Thm 3.1 | `thm:kernel` | theorem | ✅ Full | verify_kernel.py:837 | — |
| §3 eq. (3.1) | `eq:w-monotone` | equation | ✅ Full | verify_kernel.py:811 | Both halves shown load-bearing. |
| §3.1 eq. (3.2) | `eq:G-J` | equation | ✅ Full | verify_kernel.py:107 | — |
| §3.1 eq. (3.3) | `eq:J-k` | equation | ✅ Full | verify_kernel.py:140 | — |
| §3.1 Lem 3.2 | `lem:bernstein` | lemma | ✅ Full | verify_kernel.py:749 | Exact-integer second route in `check_kernel_stdlib.py`; the congruence shown load-bearing. |
| §3.1 eq. (3.4) | `eq:P-def` | equation | ✅ Full | verify_kernel.py:140 | — |
| §3.1 eq. (3.5) | `eq:P-sum` | equation | ✅ Full | verify_kernel.py:140 | — |
| §3.1 eq. (3.6) | `eq:P-coeff` | equation | ✅ Full | check_kernel_stdlib.py:125 | — |
| §3.1 eq. (3.7) | `eq:S-def` | equation | ✅ Full | verify_kernel.py:423 | — |
| §3.1 eq. (3.8) | `eq:S-table` | equation | ✅ Full | check_kernel_stdlib.py:116 | Four independent routes to `R_a(j)`. |
| §3.1 eq. (3.9) | `eq:S-R` | equation | ✅ Full | check_kernel_stdlib.py:115 | — |
| §3.1 eq. (3.10) | `eq:exp-bound-1` | equation | ✅ Full | verify_kernel.py:372 | Fails one step below its range; attained at `m = 3`. |
| §3.1 eq. (3.11) | `eq:exp-bound-0` | equation | ✅ Full | verify_kernel.py:377 | Fails one step below its range; attained at `m = 3`. |
| §3.2 Lem 3.3 | `lem:weighting` | lemma | ✅ Full | verify_monotonicity_lemmas.py:334 | Cone equivalence enumerated exhaustively for `L ≤ 5`. |
| §3.2 eq. (3.12) | `eq:abel-weight` | equation | ✅ Full | verify_monotonicity_lemmas.py:299 | — |
| §3.2 eq. (3.13) | `eq:J-weighted` | equation | ✅ Full | verify_kernel.py:459 | — |
| §3.2 eq. (3.14) | `eq:Jw-def` | equation | ✅ Full | verify_kernel.py:569 | — |
| §3.2 eq. (3.15) | `eq:B-def` | equation | ✅ Full | verify_kernel.py:542 | — |
| §3.2 eq. (3.16) | `eq:B-center` | equation | ✅ Full | verify_kernel.py:569 | — |
| §3.2 Lem 3.4 | `lem:block-sign` | lemma | ✅ Full | verify_monotonicity_lemmas.py:258 | — |
| §3.2 eq. (3.17) | `eq:H-hyperbolic` | equation | ✅ Full | verify_kernel.py:591 | — |
| §4 Cor 4.1 | `cor:C-schur` | corollary | ✅ Full | verify_theorem.py:206 | Closed range `[0, s/2]`. |
| §4 eq. (4.1) | `eq:w-from-f` | equation | ✅ Full | verify_theorem.py:197 | — |
| §4 eq. (4.2) | `eq:delta-C` | equation | ✅ Full | verify_theorem.py:124 | — |
| §4 Cor 4.2 | `cor:strict` | corollary | ✅ Full | verify_theorem.py:368 | Exact positive support `I+I`, in both the interval and infinite-support forms. |
| §4 Rmk 4.3 | `rem:local-weight` | remark | ✅ Full | check_fixed_sum_schur.py:694 | Non-log-concave witness, and failure one step below. |
| §5 Prop 5.1 | `prop:multiplicity-threshold` | proposition | ✅ Full | make_figure_multiplicity.py:193 | Both minimality clauses over the whole cone; second route in `verify_multiplicity.py`. |
| §5 eq. (5.1) | `eq:r-central-slope` | equation | ✅ Full | make_figure_multiplicity.py:129 | Sign pattern positive / exactly zero / negative at `r = 2,3,4`. |
| §5 eq. (5.2) | `eq:r-degree-three` | equation | ✅ Full | verify_multiplicity.py:445 | Closed form symbolic in `μ`; `r = 4` factorization. |
| §5 Cor 5.2 | `cor:multiplicity` | corollary | ✅ Full | verify_multiplicity.py:463 | — |
| §5 Fig 1 | `fig:multiplicity-threshold` | figure | ✅ Full | make_figure_multiplicity.py:214 | Every inlined coordinate compared character for character. |
| §6.2 Cor 6.1 | `cor:ordinary` | corollary | ✅ Full | check_proof_steps.py:253 | Majorant and Weierstrass test; both support cases. |
| §6.3 Cor 6.2 | `cor:differential` | corollary | ✅ Full | check_fixed_sum_schur.py:657 | Second route through the covariance identity in `check_structural.py`; third, integral-free route in `check_differential_coefficients.py`. |

## Dependencies

Refer to `requirements.txt`.

In addition, `render_figures.py` calls `pdflatex` and reads the result with
`PyMuPDF`.

## License

Code is made available under the MIT license (see `LICENSE.txt`), or as
otherwise noted in the comments of the file.

## References

1. D. Karp and Y. Zhang, *Log-concavity and log-convexity of series containing
   multiple Pochhammer symbols*, Fractional Calculus and Applied Analysis **27**
   (2024), no. 1, 458–486. [doi:10.1007/s13540-023-00238-0](https://doi.org/10.1007/s13540-023-00238-0)

Refer to the paper for references.
