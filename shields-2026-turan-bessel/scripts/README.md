# Computational supplement — Canonical–microcanonical positivity and phase geometry for a Bessel–₀F₁ matrix Turán determinant

Cross-check scripts for *Canonical--microcanonical positivity and phase
geometry for a Bessel--\({}_0F_1\) matrix Turán determinant*.  Every script re-derives the closed forms it
checks from the series definition of

    Z(a, λ) = Σ_{k≥0} λ^k / (k! Γ(a+k)),

so the symbolic identities are pinned to the objects that define them rather than
transcribed.  Checks are at the sharp endpoint (κ, τ) = (1, 1) unless a script
explicitly carries the deformation parameter κ or the correction parameter τ.  Symbolic work uses SymPy; numerical work uses mpmath at
arbitrary precision (no floating-point arithmetic in the verification loops).

The proofs in the paper are noncomputational and do not depend on the code here.

## Installation

Reference environment: Python 3.12, SymPy 1.14.0, mpmath 1.3.0.

```bash
python -m pip install -r requirements.txt
./run_all.sh
```

## Scripts by section

| Script | Paper section | Content |
| --- | --- | --- |
| `verify_convolution_coefficients.py` | `sec:coefficients` | Asymmetric reciprocal-gamma convolution; `S_m`; the coefficient formulas `α_m, β_m, γ_m^(κ), c_m^(κ)` and the sharp `c_m`; the eq. (matrix-series) assembly `S_m M_m` with `√g` off-diagonal; convolution moments `E_δ D, E(D Ξ), E D²`, cross-checked against direct Maclaurin coefficients of `A, B, C_κ`; and that `|δ| < a` is exactly where the convolution weights stay positive, while eq. (EDdelta) holds as an identity past it. |
| `verify_finite_law.py` | `subsec:microcanonical` (thm:ensemble-hierarchy) | The finite law `π_{m,a}`; the coefficient identities `α_m = ½E(σ−Ξ²)`, `β_m = 1−½E(D Ξ)`, `c_m^(κ) = κm/2 − ½E D²`; the identity `F_m″(0)/F_m(0) = E(Ξ²−σ) = −2ψ₁(a+m)`; and the finite-law form of `N_m`, with the conjugation `diag(1,g^{−1/2}) M_m diag(1,g^{−1/2})` of `subsec:gram` carried out from eq. (matrix-series) rather than assumed. |
| `verify_gram.py` | `subsec:gram` | The trigamma integral representation eq. (trigamma-integral) and the integrand and inequality bounds of lem:trigamma-bounds; the `ℓ²` norms `‖u‖² = ψ₁(x)`, `⟨u,v⟩ = q/(x−1)`, `‖v‖² = q²ψ₁(x−1)`; the sharp Gram-slack `(m−1)/(2(2a+2m−3))`; both Gram identities `N_m = Gram(ξ_m, η_m)` eq. (Nm-gram) and `M_m = Gram(ξ_m, √g η_m)` eq. (Mm-gram), with positive definiteness; that thm:gram's `m=1` hypothesis `a ≥ 1/2` is load-bearing (`ρ_1 < 0` for small `a`); `M_0` rank one. |
| `verify_determinant.py` | `subsec:gram`, `subsec:finite-defect`, `subsec:endpoint-sufficiency` | `MD` as the polarization of `det`; `Δ_n = ½ Σ S_k S_{n−k} MD(M_k, M_{n−k})` vs direct series; `Δ_1`; `det M_1` and the sharp anomaly threshold `a_* = 0.3690738484…`; `MD(M_1, M_m) > 0` for `0<a<1/2`; the exceptional `Δ_2 = Q_*/(2a⁶(a+1)³Γ⁴)` via `S_0 S_2 MD(M_0,M_2)+S_1² det M_1` and its positive sum-of-squares form; the κ-monotonicity identity. |
| `check_m1_indefinite.py` | `subsec:gram` (lem:M1-indefinite, eq. (det-M1)) | `det M_1` three ways -- the Gram route `ψ₁(a+1) − gβ₁²`, the closed form `((4a−1)g−4)/(4a²)`, and the 2x2 determinant; the bracket ends `f(1/4) = −4 < 0 < f(1/2) = π²/2 − 4`; `f' > 0` on `(0,1/2)` from the series against a numerical derivative, with `f'(0.7) < 0` showing that interval is load-bearing; `a²f(a) → −1`, so `f → −∞`; `a_*` to twenty digits and the ten-digit literal the paper and fig:defect-localization panel (a) both print; the inertia trichotomy from the eigenvalues of `M_1`; and a monotone sweep across the bracket whose sign change falls exactly at `a_*`. |
| `make_figure_defect_localization.py` | `subsec:finite-defect` (fig:defect-localization, subsec:finite-defect) | Every claim the figure makes: panel (a)'s classification at the endpoint -- `M_0` rank-one positive semidefinite, `M_m ≻ 0` for `m ≥ 2`, `M_1` alone changing inertia at `a_*`; `MD(M_0,M_1) = (ag−1)/a² > 0`; `MD(M_1,M_m) > 0` for `m ≥ 2` on both sides of the `a = 1/2` split in the paper's proof; the identity `MD(M_1,M_1) = 2 det M_1` that isolates the self-pair; eq. (Delta2-unsimplified); eq. (Delta-n-MD) itself against a direct series route sharing none of its code; the combinatorial claim that `(1,1)` occurs among the pairs `(k, n−k)` for `n = 2` and no other `n`; and the drawn array parsed back out of the `.tex`, checked cell by cell against that rule.  With `--emit` it also prints the array block the figure inlines. |
| `verify_degree_one_threshold.py` | `sec:main` (rem:fiber-deformation), `subsec:finite-defect` | The degree-one coefficient as the uniform threshold detector, exhibiting `{κ : Δ_1^(κ)(a) > 0 for every a > 0} = [1, ∞)` on an `a`-ladder — the observation rem:fiber-deformation records for the first nonzero determinant coefficient.  Eq. (MD01-kappa) is rebuilt from the `sec:coefficients` entries of `M_0, M_1^(κ)` and `Δ_1^(κ) = S_0 S_1 MD(M_0, M_1^(κ))`, so the sign test rests on the paper rather than on itself. |
| `verify_cauchy_mu_center.py` | `sec:phase` (lem:large-argument-limit), `sec:endpoint` | The remainder `R(μ,w) = log E_μ(w) + (4μ²−1)/(8w)` and its order derivatives `∂_μ^j R(ν,w) = O(w^{−2})`, `j ≤ 2`, at the centre `μ = ν` of `K` — the only order the proof evaluates them at — by two independent routes (Cauchy contour integral over a fixed circle vs direct numerical differentiation); the downstream limits `z·G_ν → 1`, `1+P_ν → 1`, `H^(κ) − ((2κ−1)z − κ(1+2ν)) → 0`, `D_ν^(κ) → 2(κ−1)`; the lower bound `Δ_1(a) > 1/(a⁴Γ(a)⁴)` closing `sec:endpoint`; and the fixed-`a` form of thm:critical-scaling — with `c_n = [λⁿ] gAZZ_Θ > 0`, the admissible window `min_{n≤N} Δ_n^(1)/c_n` in `1−κ` is positive for every `N` and decreasing in `N`. |
| `verify_first_negative_degree.py` | `sec:scaling` (thm:critical-scaling) | `Δ_n^(κ)` by three independent routes agreeing to 45 digits: the thm:coefficients coefficient sum, the same sum in `MD` form, and a Cauchy convolution of the termwise Maclaurin coefficients of `Z` and its derivatives that uses no Chu–Vandermonde identity. On that footing, the least degree `n_min(a,κ)` with `Δ_n^(κ) < 0` is nondecreasing in `κ` and passes the `n ≤ 60` cap, which is thm:critical-scaling's fixed-`a` claim; `κ < 1/2` makes `Δ_1^(κ) < 0` at every `a` on the grid (discriminant `2κ−1 ≤ 0`), and for `1/2 < κ < 1` the set `{a : Δ_1^(κ) > 0}` is a bounded interval, so degree one also detects failure at large `a`. |
| `verify_continuation.py` | `sec:main` (lem:continuation) | The first assertion of lem:continuation, which `sec:main` leans on twice: `Z` and its fixed-order parameter and Euler derivatives are entire in `(a,λ)`, the series converging locally uniformly. `Z` is finite at `a = 0,−1,−2,−3` where the quotient `0F1(;a;λ)/Γ(a)` is not, and agrees with it off the pole set; `∮Z da` and `∮Z dλ` vanish by Cauchy's theorem, with the same contour on `0F1` itself nonzero as a positive control that the test detects a pole when there is one; termwise differentiation is legitimate, `∂_a^j Θ_λ^l Z` agreeing across the Cauchy-integral and numerical routes; and the tail past `N` is uniformly small on `|a| ≤ 3, |λ| ≤ 2`. |
| `verify_bessel_reduction.py` | `sec:main`, `sec:phase`, App. B | `I_{a−1}(2√λ) = λ^{(a−1)/2} Z`; the bridge identities, the matrix congruence with `diag(1,2/√g)`, and the reduction `D_{a−1}(2√λ) = 4Δ/(gZ⁴)`; the boundary-correction optimality `D_{ν,R} = D_ν + G_ν(R−4/g)`; boundary limits and the rank-one `z=0` matrix; the small-`z` law (slope-2 fit) and its negative-order leg; negative-order failure `Δ_2<0` on `−2<a<−1` with the polynomial certificate, licensed by lem:coefficient-continuation at degree two — eq. (Delta2-Q) rebuilt from a Cauchy convolution of the termwise Maclaurin coefficients and required to agree with the closed form on both sides of the positive-series domain, so the appendix's evaluation at negative `a` rests on a checked identity rather than an assumed one.  Also what licenses differentiating the large-argument expansion in the lem:large-argument-limit proof: `|E_μ−1| < 1/2` uniformly on `K × Σ_δ` past a threshold `W` (with the failure in-sector below `W`, so `W` is load-bearing), eq. (log-I-two-term) at rate `w^{−2}`, the Cauchy disc `|w−z| ≤ εz ⊂ Σ_δ`, and the exponent ledger `∂_μ^j ∂_w^ℓ R = O(w^{−2−ℓ})`, `Θ_w^k R = O(w^{−2})`. |
| `verify_scalar_turanian.py` | `sec:scalar` (prop:scalar-H) | The classical scalar Turánian direction, from `I_ν` itself: eq. (R-r-def) `r_ν = zR_ν = Θ_z log I_ν − ν` and `r_ν > 0`; the Riccati step `Θ_z² log I_ν = z²+ν²−(Θ_z log I_ν)²` by two routes, the modified Bessel equation and direct differentiation; both forms of eq. (H-r-forms) against `H^(κ)` built from eq. (Hnu-kappa); eq. (H-Amos-general) as an equivalence, swept over points of both signs, with the positive root of `r²+2(ν+κ)r−z²` in its two printed shapes; eq. (H-turan-exact) and the recurrence `R_{ν+1} = R_ν^{−1} − 2(ν+1)/z` behind it; eq. (Amos-bound-exact); and eq. (H-kappa-global) both ways — `H^(κ) = H_ν + 2(κ−1)r_ν` for `κ ≥ 1`, and the small-`z` law `((κ−1)/(ν+1))z² + O(z⁴)` with the log–log slope fitted, exhibiting the failure for every `κ < 1`.  Also rem:schur-correction: the `z → 0` matrix `[[g,2],[2,4τ/g]]` and its determinant `4(τ−1)`. |
| `verify_bessel_law.py` | `subsec:microcanonical` (cor:bessel-law) | The discrete Bessel law eq. (bessel-law) and the covariance-deficit form, by three routes that share no arithmetic: the law summed term by term, derivatives of `L = log Z` with `Z = 0F1(;a;λ)/Γ(a)`, and `G_ν, P_ν, H_ν^(κ)` from `I_ν`.  That the law is a probability law and equals `(z/2)^{2k+ν}/(I_ν k! Γ(k+ν+1))`; eq. (bessel-law-meanvar) and eq. (bessel-law-param) including `L_a = −EΨ`; eq. (H-dispersion) `H^(κ) = 4(κμ−σ²)` checked as an equivalence with underdispersion `Var K < E K`; eq. (covariance-deficit-matrix) as the `diag(1,1/2)` congruence and eq. (covariance-loewner) as the same matrix minus `Cov(Ψ,K)`; and eq. (covariance-ineq) swept over a 9×6 `(a,λ)` grid, with the `τ → τ−1` boundary determinant showing the correction is load-bearing. |
| `check_tilted_tail.py` | `sec:scaling` (lem:central-moments) | The tail half of the central-moment lemma: the tilt rewriting `P(K_n=k) = π_{n,k}h_kh_{n−k}/E_{π_n}[h_Kh_{n−K}]`; `u_m → C_a` with `m_a = inf u_m > 0` and `M_a = sup u_m < ∞`; the two bracketing steps, `h_Kh_{n−K}` a positive multiple of `n^{2b}` on the central window and a bounded multiple of `n^{2b+(−b)_+}` globally, with the window's `π_n`-probability past `7/8` by the exact variance and Chebyshev; eq. (tilt-comparison) in its sharpest form, `max_k P(K_n=k)/π_{n,k}` divided by `n^{(−b)_+}` bounded above and below, so the exponent is load-bearing; Hoeffding against the exact hypergeometric tail; eq. (tilted-tail) as the exact tail moment under the assembled bound, plus the effective-exponent test that shows that bound super-polynomially small; and eq. (X4-asymptotic) under the tilted law. |
| `check_vertical_wall_transfer.py` | `sec:scaling` (thm:wall-fan) | Whether the coefficient–argument matching survives varying `τ`, by checking the theorem's own chain: eq. (pq-coefficients) against `p_n, q_n` built by Cauchy convolution; `p_n = T_nEα_{K_n}` with `Eα_{K_n} = 2/n + O(n^{−2})` and `q_n = (gT_n/2)E[(n−K_n)α_{K_n}]`; `p_n/q_n = 4/(gn) + O(n^{−2})`; eq. (vertical-wall-two-term) `1 − κ_{n,τ} = c_τ(a)/n + O(n^{−2})` at five values of `τ`; the argument-side limit `c_τ(a) = lim_z zD^{(1,τ)}_{a−1}(z)` from `I_ν`; and the multicritical `τ_∞(a) = g(a−7/8)` at which both leading terms vanish. |
| `check_scaling_expansions.py` | App. A (eq:Fm-boundary), `sec:scaling` (eq:Phi1, eq:Phi2, eq:MD-expectation-two-term), `sec:phase` (eq:D-large-z-refined) | The three closed forms the text displays without deriving in full, each rebuilt from the quantity it comes from: eq. (Fm-boundary) itself, rebuilt from eq. (Nhat-m) and the `α, β, c` formulas; `EΦ₁` and `EΦ₂` from eq. (Phi1), eq. (Phi2) and the central moments, then checked to reassemble the `1/n²` coefficient of eq. (MD-expectation-two-term) that the paper states separately; and the `ε`-expansion of `D^{(1−ε,1)}` from eq. (D-large-z-refined), with the rescaled limit `−2 + c(a)/s`, its simple zero at `s = c(a)/2`, and the `O_a(ε)` remainder compared against the tighter of the two sign margins at `η = √ε` — the comparison that makes the IVT bracket legitimate, and which `η = ε` fails.  Exact symbolic arithmetic, `g` carried as a symbol. |
| `verify_context.py` | `sec:context` | The infinitesimal Turánian `Z(a+δ)Z(a−δ)−Z² = −Aδ²+O(δ⁴)`, with the evenness in `δ` that rules out a `δ³` term; that the `F(ν,y)` forms agree with eqs. (Gnu), (Pnu), (Hnu) under `z = e^y`; and the rescaling variance: `e^{cy}` fixes `G_ν,P_ν` but shifts `D_ν` by `+2cG_ν`, so any `c < −D_ν/(2G_ν)` makes it negative and the sign is not rescaling-invariant. |
| `check_coefficients_stdlib.py` | `subsec:endpoint-sufficiency` (lem:Delta2-positive) | Dependency-free audit of `exported_coefficients.json`: expands the three blocks of eq. (Qstar-decomp) in exact integer arithmetic and checks the stored powers and coefficients against them, plus integrality and strict positivity.  With eqs. (Delta2-Q) and (Qstar-decomp) that gives `Δ_2 > 0`; the identities themselves are `verify_determinant.py`'s. |
| `check_two_parameter_affine.py` | `sec:main` | The two-parameter family assembled by Cauchy convolution of the termwise coefficients of `Z, Z_a, Z_aa, Z_Θ, Z_ΘΘ, Z_aΘ`, with no Chu–Vandermonde input: the affine identity `Δ^(κ,τ) = Δ + (κ−1)Q + (τ−1)P` coefficientwise, strict positivity of `P` and `Q` in every positive degree, the constant term `g(τ−1)/Γ(a)⁴`, and the two-parameter congruence `D_ν^(κ,τ)(2√λ) = 4Δ^(κ,τ)/(gZ⁴)` against `I_ν` itself. |
| `check_schur_boundary_limit.py` | `sec:main` (cor:bessel-matrix, eq:Delta0-tau), `sec:scalar` (rem:schur-correction) | The entry-level inputs to the `z → 0` limit, which `verify_scalar_turanian.py` and `verify_bessel_reduction.py` reach from `I_ν` and from the reduction: each series at `λ = 0` is its own degree-zero coefficient — `A = g/Γ(a)²`, `B = 1/Γ(a)²`, `C_{κ,τ} = τ/Γ(a)²`, `Z = 1/Γ(a)`, none of them depending on `κ` — and the normalized degree-zero sector is `2(τ−1)`, which is eq. (Delta0-tau).  The matrix limit `[[g,2],[2,4τ/g]]`, its determinant `4(τ−1)`, and rank one exactly at `τ = 1` are re-checked alongside as a consistency test. |
| `check_hkappa_zform.py` | `sec:scalar` (prop:scalar-H, eq:H-r-forms, eq:H-turan-exact, eq:H-kappa-global), `sec:main` (eq:Hnu-kappa, eq:G-L) | The route that reads the second diagonal direction as a Turánian in the reciprocal-gamma series rather than in the Bessel ratio: the Euler-series parameter shifts `Z_Θ = λZ(a+1)` and `Z_ΘΘ = λ(λZ(a+2) + Z(a+1))`, whence `H^(κ) = 4λ[(κ−1)Z₁/Z − λ(Z Z₂ − Z₁²)/Z²]`, checked against `H^(κ)` built from `r_ν` by eq. (H-r-forms).  Then the three consequences that route turns eq. (H-kappa-global) into: `Z₁² > Z Z₂`, the midpoint case of concavity of `a ↦ log Z`, which therefore follows from the paper's own `A/Z² > 0`; the shift `H^(κ) − H^(1) = 4(κ−1)λZ₁/Z`; and the `λ → 0` value `(κ−1)/a` of the bracket, which is where every `κ < 1` fails. |
| `verify_phase_diagram.py` | `sec:phase` | The refined large-argument expansion, derived from the two-term Hankel expansion rather than quoted, and its endpoint constant `c(a) = 4/g − 4a + 7/2`, with `3/2 < c(a) < 7/2`; `τ_cw` as the exact degree-one root, the form `τ_* − (κ−1)q_1/p_1`, and `τ_cw < 1`; every step of the boundary lemma — `∂L_m/∂m`, the `m → ∞` limit and `L_2` with the exact trigamma substitutions `−2a`, `−a`, `−(a²+1)/(3a)`, the `F_m` formula, `det N̂_1`, `s_*+c_2`, the `m=2` reduction, and the boundary `Δ̂_2` with its polynomial `P_2`; the ratio inequalities `q_n/p_n > gn/4 > q_1/p_1`; the boundary lemma and the phase-diagram theorem on both sides of `τ_cw` across a 14-point `a`-ladder; the limits `4(τ−1)` at `z → 0` and `2(κ−1)` at `z → ∞`; and the one-zero strip, root bracketed numerically. |
| `verify_critical_scaling.py` | `sec:scaling` | The duplication rewrite of `S_m` and its gamma-ratio expansion (`C_a`, `b`, `s_a`, tested to `m = 6400`); the large-`m` expansions of `α_m, β_m, c_m`; the central expansion of `MD(M_k, M_l)` with `Φ₁(x)` and `Φ₂(x)` recovered rather than assumed; the exact hypergeometric moments for `Hyp(2n,n,n)` and `E X_n²` under the true tilted law; the averaging and the division that yields `n(1−κ_n) = c(a) + (g−24)/(12g)/n + o(n⁻¹)`; the saddle relation `z = 2√λ ∼ n/2`; and the law itself, computed from exact coefficients out to degree 400 and Richardson-extrapolated against `c(a)` and `1/12 − 2/ψ₁(a)`, with `κ_n` eventually increasing and `N_a(κ) ∼ c(a)/(1−κ)`. |
| `check_boundary_polynomial_certificate.py` | `sec:phase` (lem:boundary-positivity), App. A | The `a > 1/2` branch of the boundary lemma. The proof has no convexity step and no derivative certificate — the whole argument is that `B_{a,m}` is increasing in `g` — so that is what is checked: the telescoping bound `Σ_{r<m}(a+r)^{−2} < (m−1)/[(a+½)(a+m−½)]` reducing `F_m > 0` to eq. (Bm-boundary); both `g`-dependent pieces of `B` increasing, the second with derivative numerator `a³(16a−8)`, positive exactly on `a > ½` — which is why `a = ½` is excluded and handled separately; that `2a²g₀ − 1 = 2a` exactly, so `s_*(g₀) = A` and the bound `s_* < A` is the `g → g₀` substitution rather than a second estimate; the resulting `B > P(ã,m̃)/[(ã+1)(2ã+1)²(ã+m̃+1)(ã+m̃+2)(2ã+2m̃+3)]` as an identity with the printed `P` and denominator, in the shifted variables `ã = a−½`, `m̃ = m−2`; and strict positivity of every coefficient. Also the two branches the certificate does not cover — `F_m = π²m/8 − 2(m−1)/(2m−1) > 0` at `a = ½`, and the `m = 2` margin `2(2a³−a²+1) > 0` for `0 < a < ½`. |
| `make_figure_wall_fan.py` | `sec:scaling` (fig:wall-fan, thm:wall-fan, cor:wall-orientation) | Every claim the figure and its caption make about the coefficient-wall fan at `a = 1`.  The plotted lines are the exact degree walls eq. (coefficient-wall) `τ_n(a,κ) = 1 − (Δ_n + (κ−1)q_n)/p_n`, not their asymptotic approximations: `Δ_n, p_n, q_n` are built twice by routes sharing no code — from the endpoint fibers `M_m`, and by direct Cauchy multiplication of the Maclaurin series of `Z, Z_a, Z_aa, Z_Θ, Z_ΘΘ, Z_aΘ` — and required to agree, which is what licenses the word *exact* in the caption.  Also the closed forms the figure prints, `τ_∞(1) = ψ₁(1)/8 = π²/48` and `τ_cw(1,1) = π²/(2(π²−3))`; `τ_n(1,1) ↓ τ_∞(1)`, the `a > 11/12` side of eq. (wall-orientation); `τ_1(1,κ) = τ_cw(1,κ)` identically in `κ`, and `τ_1` the strict maximum over plotted degrees at every `κ ≥ 1`, so degree one is the exposed wall; eq. (wall-ordering) at `a = 1`; the slope identity `∂_κ τ_n = −q_n/p_n` with the strict ordering `q_n/p_n > gn/4` of eq. (qn-pn-ratio) and the asymptotic `−ψ₁(1)n/4` the caption quotes; the `κ < 1` divergence the figure displays, walls eventually increasing in `n` and outgrowing `(gn/4)(1−κ)`; and that the drawn window is honest — the `n = 1` wall and both marked levels interior, every wall falling left to right, and every line leaving the box only through an edge `pgfplots` clips.  With `--emit` it also prints the line block the figure inlines. |
| `check_wall_fan.py` | `sec:scaling` (thm:wall-fan, prop:c-monotone) | The full two-term deformation wall for every fixed `τ`, of which the endpoint expansion is the `τ = 1` slice, with both sides built independently: eq. (vertical-wall-two-term) `1 − κ_{n,τ} = c_τ/n + d_τ/n² + O(n^{−3})` at nine `(a,τ)` pairs from `Δ_n, p_n, q_n` by Cauchy convolution; eq. (pq-ratio-two-term) `p_n/q_n = 4/(gn) − 2/(gn²) + O(n^{−3})`, the ratio that makes the second term available; eq. (D-wall-two-term) `c_τ/z + e_τ/z²` from `log I_ν` directly; eq. (tau-n-limit) `τ_n(a,1) → τ_∞(a)`; `c_τ = (4/g)(τ − τ_∞)` as a signed distance, so the leading jet vanishes exactly at `τ_∞`; eq. (second-order-match) on both sides, `2(11/12−a)/n²` against `(11/12−a)/z²`; eq. (wall-ordering) `τ_∞(a) < τ_cw(a,1) < 1` across `a ∈ [0.05, 50]`; eq. (wall-slope-covariance) `q_n/p_n = gn/4 − (g/2)Cov(K_n, α_{K_n})/Eα_{K_n}` as an exact identity, with `Cov < 0` because `α` is decreasing, so the strict slope ordering that keeps degree one exposed is a negative-covariance effect rather than a pairing accident; and all four displays of cor:wall-orientation — the `1/n` law whose sign flips at `a = 11/12` (checked as a flip, not just a rate), the transverse `O(n)` slope with its `κ ≷ 1` trichotomy, and the tangent-cone limit, whose inversion for `x` returns `c_τ`.  Also prop:c-monotone — `ψ₁² + ψ₂ > 0`, `c` strictly decreasing, the limits `7/2` and `3/2`, and the two kernel inequalities `t/(1−e^{−t}) > 1 + t/2` and `(k∗k)(s) > s²/(1−e^{−s})` the proof rests on. |
| `check_cubic_multicritical.py` | `sec:scaling` (thm:cubic-multicritical) | The two constants at the point where both lower wall jets vanish, each from exact data rather than from the tilted-moment expansions the proof assembles: that `τ_mc = ψ₁(11/12)/24` is exactly `τ_∞(11/12)` and `ν_mc = a_mc − 1`, so the point is the one the fan singles out; eq. (cubic-coeff-scaling) `1 − κ_{n,τ_mc} = 3/(8n³) + O(n^{−4})`, with `n³(1−κ_n)` decreasing monotonically to `3/8` out to `n = 2400`; eq. (cubic-bessel-scaling) `D_{−1/12}^{(1,τ_mc)}(z) = 3/(32z³) + O(z^{−4})` from `log I_ν`, likewise monotone to `3/32`; both lower jets vanishing on both sides; that `3/(8n³)` at `n = 2z` is half of `3/(32z³)` — the same factor `2` that already relates the first and second jets, so the dictionary is uniform across all three orders rather than rescued at the third; eq. (critical-wall-orientation), the `3g_mc/(32n²)` rate at which the wall sits below `τ_mc` once the `1/n` term is gone; rem:jet-transfer-factor `A_r^can = 2^{1−r}A_r^mic` at `r = 1, 2, 3`; and eq. (log-I-four-term), whose four-term residual times `z⁵` settles to a constant at five orders. |
| `check_ensemble_hierarchy.py` | `sec:coefficients` (thm:ensemble-hierarchy, prop:four-copy, rem:fiber-deformation, rem:ensemble-positivity) | The identities that make the coefficient matrices conditioned Bessel fibers rather than an analogy, each checked as the exact identity it is: eq. (fiber-affine) `M_m^{(κ,τ)} = M_m + [(τ−1) + (gm/2)(κ−1)]e₂e₂ᵀ` at every fiber including the two exceptional ones, which is what makes `Δ_n` affine in `(κ,τ)`; eq. (pair-total-law), eq. (finite-law-entries) and eq. (four-total-law), with `Z²` and `Z⁴` as the two- and four-copy generating functions and the conditioned fibers verified to be probability laws; eq. (canonical-fiber-average) `T/Z² = E M_R`; eq. (Delta-n-MD) `Δ_n/T_n = ½E MD(M_{K_n}, M_{n−K_n})`; eq. (D-canonical-average) `D = E d_N`, the identity separating coefficientwise from pointwise positivity; eq. (sector-density) at both ends, `d₀ = 4(τ−1)` exactly and `d_n ↓ 2(κ−1)`; eq. (N-mean) `E N = 2(Θ_z log I_ν − ν)` against the Bessel congruence, closing the loop; and eq. (microcanonical-covariance-deficit), the normalized fiber as a covariance deficit in the conditioned law — the identity that makes cor:bessel-law the averaged version of the same structure rather than a separate calculation. |
| `check_fiber_moment_identities.py` | `subsec:microcanonical` (thm:ensemble-hierarchy) and `subsec:gram` (thm:gram) | The layer beneath eq. (finite-law-entries): the four fiber moments in the unnormalized form a formal proof consumes, rather than the normalized form the paper states.  The score sum `∑_i (m−2i)Ξ_i w_i` in the closed form lem:convolution gives it, and its normalization eq. (EDXi) `E(DΞ) = m/(a+m−1)`; `E D² = m(2a+m−2)/(2a+2m−3)` through `S_m(a) E[I(m−I)] = S_{m−2}(a+1)`, the `i(m−i)` cancellation against the two factorials; the curvature sum `∑_i w_i(Ξ_i² − σ_i) = −2S_mψ₁(a+m)`, which is eq. (F-second-delta) read on the finite sum rather than on the closed form eq. (Fdelta); and the two-center weight derivative at the two centers those sums need, since the symmetric center of eq. (Fdelta) is not the one inside the sum.  Also eq. (Nm-gram) at general `s`, not only at the endpoint shift `s = 1/g`: `N_m(s)` is the Gram matrix of `ξ_m = (u,0)` and `η_m(s) = (v,√ρ_m(s))`, its `(2,2)` entry is `s + c_m` for every `s`, and it is positive definite exactly where `ρ_m(s) > 0`. |
| `check_trigamma_cubic_bound.py` | `subsec:gram` (lem:trigamma-bounds) | The cubic upper bound `ψ₁(y) < 1/y + 1/(2y²) + 1/(6y³)`: the integrand inequality `x coth x < 1 + x²/3` and its sharpness at the origin, the split of `τ/(1−e^{−τ})` the proof uses, the Laplace transform that turns the majorant into the bound exactly, a numerical ladder across eight decades, and that the bound is load-bearing — the half-shift bound `1/(y−1/2)` puts the `L_2` numerator at exactly `(a−1)/(2a−1) ≥ 0` for `a ≥ 1`, proving nothing there, while the cubic bound puts it at exactly `−(a²+1)/(3a) < 0`. |
| `render_figures.py` | all figures | Builds the separate artwork file `shields-2026-turan-bessel-figure.pdf` the submission portal wants, one figure per page, by extracting each float from the manuscript itself rather than from a hand-kept copy — so the artwork cannot drift from the paper.  Handles both shapes the paper uses: a bare `tikzpicture` schematic and a `pgfplots` axis.  The governing invariant is that the number of figure floats in the manuscript equals the number of pages rendered, checked against the built PDF, so a float added to the paper and forgotten here fails this script instead of silently shipping incomplete artwork; the per-figure plot counts, landmark text and vector-path floors are asserted alongside, and a raster image on any page is an error.  The float-count and plot-count invariants are pure Python and always run; the render itself is skipped with a message where `pdflatex` or `PyMuPDF` is absent, since `requirements.txt` pins only `sympy` and `mpmath`. |
| `check_gamma_ratio_expansion.py` | `sec:scaling` (eq:Sm-gamma-ratio, eq:Sm-asymptotic) | The constants `C_a`, `b`, `s_a` of eq. (Sm-asymptotic), which the paper quotes from the DLMF and does not derive, together with the explicit-remainder route to them.  The four shifts of eq. (Sm-gamma-ratio) carry signs `+,-,+,-`: their signed sum vanishes, `sum(eps c)` is `b` and `sum(eps (c^2-c)/2)` is `s_a`, all three computed symbolically from the shifts alone and then measured against the exact `S_m` -- so `C_a = 2^(2a-2)/sqrt(pi)` is read off an identity that holds to 59 digits rather than copied.  Then the two steps a formalization needs: the two-point Stirling sandwich `0 <= log Gamma(y) - log Gamma(x) - (L(y)-L(x)) <= 1/(120 x^2)` for `L(t) = t log t - t - (log t)/2 + 1/(12t)`, whose derivative is exactly the digamma gap of lem:trigamma-bounds -- which is what makes Stirling's additive constant cancel in a difference -- and the per-shift bound `|W(m,c) - (c^2-c)/(2m)| <= 5(|c|+1)^3/m^2` on `m >= 4|c|+4`.  The order is checked sharp, not merely sufficient: `m^2(S_m/(C_a 4^m/(m!)^2 m^b) - 1 - s_a/m)` converges to a finite nonzero limit at every tested `a`.  Also the weaker constants the `lean/` statement carries, at the threshold where they are tightest. |
| `check_polygamma_positivity.py` | `sec:scaling` (prop:c-monotone) | The recurrence route to `psi_1^2 + psi_2 > 0`, which check_wall_fan.py checks by the paper's Laplace-convolution argument.  The point is that the obvious route is blocked and the working one is tight: `P = psi_1^2 + psi_2` is `O(y^-4)`, the three-term expansions of the two sides cancelling through `y^-3` exactly, so no comparison of the two sandwiches decides anything; the exact one-step drop `P(y) - P(y+1) = 1/y^4 - 2/y^3 + 2 psi_1(y+1)/y^2` is positive by eq. (trig-lower) at `y+1`, and by the exact margin `(2y+3)y^2 - (2y-1)(y+1)^2 = 1`, while the crude `psi_1(y+1) > 1/(y+1)` falls short for every `y > 1`; and `P(y+n) -> 0`, so the descent pins `P > 0` rather than merely `P` decreasing.  Then `c'(a) = -4(psi_1^2+psi_2)/psi_1^2`, so `c' < 0` is that inequality and nothing else. |
| `check_hypergeometric_variance_route.py` | `sec:scaling` (lem:central-moments, eq:hypergeom-moments) | The two binomial identities the exact variance is assembled from, which verify_critical_scaling.py quotes the conclusion of: `sum_k k^2 C(n,k)^2 = n^2 C(2n-2,n-1)`, which is `k C(n,k) = n C(n-1,k-1)` squared termwise and then Chu-Vandermonde on the shifted row, and `n C(2n,n) = 2(2n-1) C(2n-2,n-1)`, the consecutive-central-binomial ratio that turns it into a closed form.  Beside them the two they sit with, `sum_k C(n,k)^2 = C(2n,n)` and the reflection giving `E K = n/2`, and then the law summed term by term against `E K^2 = n^3/(2(2n-1))` and `Var K = n^2/(4(2n-1))`.  Exact rationals: the variance is a cancellation of two quantities of size `n^2` down to one of size `n/8`. |

Each script prints `PASS` lines and ends with `ALL PASS`; any broken identity raises
`AssertionError` and stops the script with a nonzero exit code.

Script headers name the paper section a script covers, and cite every result and
displayed equation inside it by LaTeX label rather than by number, since the numbers
move when the paper is renumbered and the labels do not.

## Dependencies

Refer to `requirements.txt`.

In addition, `render_figures.py` calls `pdflatex` and reads the result with
`PyMuPDF`.

## License

Code is made available under the MIT license (see `LICENSE.txt`), or as
otherwise noted in the comments of the file.

## References

1. D. Karp and Y. Zhang, *Log-concavity and log-convexity of series containing
   multiple Pochhammer symbols*, Fractional Calculus and Applied Analysis **27**
   (2024), 458–486. [doi:10.1007/s13540-023-00238-0](https://doi.org/10.1007/s13540-023-00238-0)

Refer to the paper for references.
